services:
  dozzle:
    image: amir20/dozzle:latest
    container_name: citadel-logs
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      DOZZLE_NO_ANALYTICS: "true"
